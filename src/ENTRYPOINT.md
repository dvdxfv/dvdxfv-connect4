# Runtime entrypoint

- Portable startup executable (local build artifact): repository root `*.exe` that is not installer.
- Installer executable (local build artifact): repository root `*setup*.exe` or `*安装包*.exe`.

The repository does not commit large binaries; packaged executables are distributed in GitHub Releases assets.
